#!/usr/bin/env groovy

pipeline {
    agent {
        kubernetes {
            yamlFile 'KubernetesPod.yaml'
            retries 2
        }
    }
            
    stages {
        stage('Run Python Script') {
            steps {
                sh 'python --version'
            }
        }
    }

}
